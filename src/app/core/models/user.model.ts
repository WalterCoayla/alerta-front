// src/app/models/user.model.ts
export interface User {
  id: number;
  name: string;
  email: string;
  // Añade otros campos que tu modelo User de Laravel tenga y que necesites en el frontend
}

export class User {
}
